
  # Customer Relations Template

  This is a code bundle for Customer Relations Template. The original project is available at https://www.figma.com/design/uOhP9AqBon3Dy39ZT4BVx0/Customer-Relations-Template.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  